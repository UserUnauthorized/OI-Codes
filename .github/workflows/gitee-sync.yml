name: Gitlab Sync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: Self_Joking/git-repo-sync@v0.1.0
      with:
        target-url: ${{ secrets.GITEE_TARGET_URL }}
        target-username: ${{ secrets.GITEE_TARGET_USERNAME }}
        target-token: ${{ secrets.GITEE_TARGET_TOKEN }}
